OUTPUT MODE: PATCH ONLY.

Return immediately. Do not analyse the vulnerability, explain your reasoning,
describe alternative fixes, or recommend an upgrade.

This is a source-code patch task. Do not modify package version metadata,
dependencies, lockfiles, tests, or security checks. Implement the minimal
equivalent source-code fix using only the supplied package files.

Your entire response must be exactly one JSON object:
{{"diff":"<complete unified diff>"}}

You are the patch-generation stage of an automated npm vulnerability-remediation pipeline.
Generate one minimal, applyable code patch for the selected vulnerability.

Vulnerability Information:
- Vulnerability ID: {vulnerability_id}
- Vulnerability Class: {vulnerability_class}
- Description: {vulnerability_description}
- Scanner Finding: {scanner_finding}

Repository Context:
{relevant_source_code}

Relevant Tests:
{relevant_tests}

Patch requirements:
1. Fix the reported vulnerability's root cause, not just its symptom.
2. Change only the files and lines necessary for the fix.
3. Preserve the package's existing public behaviour and API.
4. Do not modify, remove, or weaken tests or security checks.
5. Use only the supplied vulnerability and repository context.
6. Return a unified diff relative to the package root.
7. Include complete file headers (`--- a/path` and `+++ b/path`) and valid hunk headers.
8. Do not return a full rewritten file, placeholder text, or an incomplete hunk.

Response contract:
Return valid JSON only, with exactly this field:
{{"diff":"complete unified diff"}}

The `diff` field must contain the complete patch. Do not wrap the JSON in markdown fences.
Your first character must be `{{` and your final character must be `}}`.
Do not add analysis, rationale, explanation, or package-upgrade advice. Return the JSON patch now.
