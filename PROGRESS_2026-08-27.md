# Progress Log - 2026-08-27

## Scope worked on today

Marcus scope only:
- package input
- vulnerability detection
- patch generation

Teammate scope intentionally not touched:
- patch application
- patch validation
- outcome classification

## Proposal and skeleton review

- Reviewed `Proposal_CS480_V2.pdf` and confirmed the intended pipeline is:
  `package_input -> vulnerability_detection -> patch_generation -> patch_application -> patch_validation -> outcome_classification`
- Reviewed the repo skeleton and confirmed only `graph.py`, `state.py`, `README.md`, and `langgraph.json` had meaningful structure at the start.
- Confirmed `package_input.py`, `vulnerability_detection.py`, and `patch_generation.py` were still stubs before implementation.

## Design decisions made

- Deferred broader shared-graph cleanup until later coordination with teammate.
- Proceeded on Marcus branch with implementation of stages 1 to 3 only.
- Kept outputs explicit so downstream stages can consume them later without guessing.
- Chose `npm pack` for package ingestion so the exact published package version can be fetched and unpacked reproducibly.
- Kept `Syft + Grype` as the vulnerability detection interface because it matches the proposal.
- Made patch generation output a structured patch artifact with a unified diff target format.

## Code changes completed

### `package_input.py`

- Implemented input validation for `package_name` and `package_version`.
- Added npm executable resolution for Windows (`npm.cmd` fallback).
- Added package download using `npm pack`.
- Added tarball extraction into `.workspace/packages/...`.
- Added detection of extracted package root and `package.json`.
- Added structured outputs:
  - `source_dir`
  - `package_manifest_path`
  - `tarball_path`

### `vulnerability_detection.py`

- Implemented Syft runner.
- Implemented Grype runner.
- Implemented normalization of Grype results into the shared vulnerability structure.
- Added repo-local cache handling under `.workspace/cache` to avoid Windows permission failures in default user cache locations.
- Added explicit early-stop behavior when Syft returns an SBOM with no artifacts.
- Added scan artifact outputs:
  - `scan_artifacts["syft_json"]`
  - `scan_artifacts["grype_json"]` when available

### `patch_generation.py`

- Implemented target vulnerability selection for first-pass patch generation.
- Added lightweight context collection from `package.json` and likely entry files.
- Added OpenAI client integration using `OPENAI_API_KEY`.
- Enforced structured JSON output from the model.
- Added structured patch outputs:
  - `patch_attempts`
  - `current_patch`
  - `current_patch.diff`
  - `current_patch.rationale`
  - `current_patch.target_files`

### `state.py`

- Extended state definitions for Marcus-side stages with:
  - `source_dir`
  - `package_manifest_path`
  - `tarball_path`
  - `scan_artifacts`
  - `advisory_url`
  - `aliases`
  - `target_files`

### `requirements.txt`

- Added `openai`

## Tests added

Added pytest coverage for the three implemented stages:
- `tests/test_package_input.py`
- `tests/test_vulnerability_detection.py`
- `tests/test_patch_generation.py`
- `tests/conftest.py`

Current automated test result:

```bash
pytest tests/test_package_input.py tests/test_vulnerability_detection.py tests/test_patch_generation.py -q
7 passed
```

## Live environment findings

### Tooling confirmed installed

- `node`
- `npm`
- `syft 1.45.1`
- `grype 0.114.0`

### Real package ingestion test

- Successfully fetched and unpacked `lodash@4.17.15` using `package_input`.
- Extracted package path:
  `.workspace/packages/lodash/4.17.15/source/package`

### Real vulnerability detection test

- Syft can run successfully after moving cache/config under `.workspace/cache`.
- For `lodash@4.17.15`, Syft produced an SBOM with `artifacts: []`.
- Because of that, the node now returns:
  `vulnerability_detection: syft produced an SBOM with no artifacts for this package input`
- This means the current `Syft -> Grype` flow is not yet surfacing an npm package artifact for this package input style.

### Real patch generation test

- Not fully live-tested yet because `OPENAI_API_KEY` was not available in the current execution environment.
- Mocked and unit-tested behavior is passing.

## Key conclusion from today

Implementation progress is good, but the main open issue is no longer basic code scaffolding. The important unresolved problem is detection strategy:

- current ingestion works
- current unit tests pass
- current live Syft scan does not identify `lodash@4.17.15` as an artifact in the expected way

So the next major task is likely refining npm vulnerability detection strategy while keeping the project aligned with the proposal.

## Suggested next steps

1. Decide whether to adapt the scan target or supplement `Syft + Grype` for npm package detection.
2. Live-test `patch_generation` once `OPENAI_API_KEY` is available.
3. After alignment with teammate, apply the minimal shared graph/state integration cleanup in `graph.py`.
